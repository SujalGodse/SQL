--1
-- SELECT DISTINCT ENAME,SAL,SAL*12 AS ANNUAL_SAL
-- FROM SCOTT.EMP
-- WHERE SAL BETWEEN 2000 AND 3000 AND 
--         SAL*12 >29400;

--2
-- SELECT ENAME 
-- FROM SCOTT.EMP
-- WHERE DEPTNO IN (10,40,30,20) AND
--         JOB NOT IN ('SALESMAN','MANAGER') AND
--         HIREDATE > '31-DEC-82';

--3
-- SELECT SCOTT.EMP.*,SAL*6 AS HALF_TERM_SAL
-- FROM SCOTT.EMP
-- WHERE (HIREDATE LIKE '%FEB%' OR HIREDATE LIKE '%DEC%') AND MGR NOT BETWEEN 7369 AND 7521

--4
-- SELECT COUNT(*),DEPTNO
-- FROM SCOTT.EMP
-- WHERE HIREDATE LIKE '%81' AND ENAME LIKE '%E%'
-- GROUP BY DEPTNO;

--5
-- SELECT COUNT(*),MAX(SAL),MIN(SAL),SUM(SAL),DEPTNO
-- FROM SCOTT.EMP
-- WHERE COMM IS NULL AND 
--         MGR IS NOT NULL
-- GROUP BY DEPTNO
-- HAVING COUNT(*)>=2 AND 
--         MAX(SAL)>2450 AND 
--         MIN(SAL) IN (800,950) AND
--         (SUM(SAL) LIKE '____' OR
--         SUM(SAL) LIKE '_____');

--6
-- SELECT COUNT(*),JOB
-- FROM SCOTT.EMP
-- WHERE (ENAME LIKE '%L%' OR 
--         ENAME LIKE '%N%')
-- GROUP BY JOB
-- HAVING COUNT(*)>=2 AND  
--         MAX(SAL) LIKE '____' AND 
--         MIN(SAL) NOT BETWEEN 100 AND 1000
-- ORDER BY JOB;

--7
-- SELECT 'MR. '||ENAME||' YOU HIRED ON '||HIREDATE||' AND WORKING AS A '||JOB||' WITH 23% HIKE OF SALARY '||SAL*1.23
-- FROM SCOTT.EMP
-- WHERE HIREDATE<'1-JAN-82' AND HIREDATE NOT BETWEEN '01-SEP-81' AND '30-SEP-81' AND JOB IN ('ANALYST','PRESIDENT');

--8
-- SELECT ENAME,SAL
-- FROM SCOTT.EMP
-- WHERE (ENAME LIKE '%A%' OR ENAME LIKE '%E%' OR ENAME LIKE '%I%' OR ENAME LIKE '%O%' OR ENAME LIKE '%U%') AND 
--         DEPTNO!=20 AND 
--         SAL>=2450 AND
--         SAL<=3000
-- ORDER BY ENAME DESC;

